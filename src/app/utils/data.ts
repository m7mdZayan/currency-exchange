export const POLLING_INTERVAL = 60000; // 60 seconds
// is's the time that we will wait to update the data

export const rowsPerPageOptions = [5, 10, 25];
// is's the available options to show the number of rows per page
